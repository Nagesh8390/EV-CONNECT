# ── Database Configuration ──
# Uses local MySQL by default, but can be overridden by environment variables on Render/Railway
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/evconnect}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.datasource.driver-class-name=${DATABASE_DRIVER:com.mysql.cj.jdbc.Driver}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQLDialect}

server.port=${PORT:8080}
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# ── Gmail SMTP Email Configuration ──
# REPLACE the values below with your real Gmail address and App Password.
# Or use environment variables for better security during deployment.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:evconnect.service@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bxtt cedd ljvn swsw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
